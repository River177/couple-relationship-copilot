# Infrastructure

This folder contains environment/deployment assets only, for example:

- `minio-compose.yml` for local object storage setup

Do **not** place DB schema source files here.
DB schema source of truth is in `api/sql/`.
