# Engineering Plan (Step-1 bootstrap)

Done:
- FastAPI skeleton with core routers
- Flutter manual starter structure (pending local Flutter install)
- PostgreSQL MVP schema draft (`api/sql/001_init_schema.sql`)

Next:
1. Add SQLAlchemy models + migration setup (Alembic)
2. Implement CRUD for daily entries
3. Integrate MemOS write/read service in API
