# 项目进展与后续计划（2026-03-03）

## 一、今日新增进展

### 1) 数据库重构（已落库）
- 新增迁移：`api/sql/002_auth_and_binding_refactor.sql`
- 新增/增强：
  - `auth_sessions`
  - `auth_verification_codes`
  - `relationship_invites`
  - `users`（补充 `avatar_url`、`status`）
  - `couples`（状态与唯一约束增强）
- 目标：支持“登录态 + 邀请绑定”，并确保内部 ID 不暴露给用户输入。

### 2) 后端接口（已完成并通过本地联调）
新增接口：
- `POST /auth/send-code`
- `POST /auth/login`
- `POST /auth/refresh`
- `GET /auth/me`
- `POST /relationship/invite`
- `POST /relationship/join`
- `GET /relationship/status`
- `POST /relationship/unbind`

实现文件：
- `api/app/routers/auth.py`
- `api/app/routers/relationship.py`
- `api/app/deps/auth.py`
- `api/app/core/security.py`
- `api/app/core/config.py`
- `api/app/main.py`

### 3) 前端主流程改造（已完成）
- 登录页 + 验证码登录
- 本地会话持久化（access/refresh token）
- 启动时自动恢复登录态
- 绑定页支持生成邀请码、输入邀请码绑定
- 首页展示绑定状态与解绑能力

实现文件：
- `app/lib/main.dart`
- `app/pubspec.yaml`

---

## 二、当前未收口项（后续继续）

1. 日常记录页：
   - 仍需彻底改为“登录态 + 当前关系”自动参数，去除遗留手填 ID 路径。
2. 冲突调解页：
   - 同上，去除 `session_id/user_id/couple_id` 直填流程。
3. 前端代码结构：
   - 当前逻辑集中在 `main.dart`，后续需拆分模块化（page/service/model/state）。

---

## 三、本次提交目的

- 先把“数据库 + 后端认证绑定 + 前端主流程”的阶段性成果固定到 GitHub。
- 避免本地改动长期悬空，便于后续在此基础上继续迭代。

---

## 四、下一步建议

1. 先改完日常记录页自动流转（不填 ID）
2. 再改冲突调解页自动流转（不填 ID）
3. 补一轮端到端验收脚本，确保登录→绑定→记录→调解全链路可跑通
