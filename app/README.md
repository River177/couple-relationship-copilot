# Mobile App (Flutter)

Flutter CLI is not installed on this machine yet, so this is a manual starter structure.

## After installing Flutter

```bash
flutter --version
cd app
flutter create .
flutter pub get
flutter run
```

Then keep `lib/main.dart` as project entry and proceed with feature modules.
